CREATE TABLE images_uploaded(time TIMESTAMP, entry INT(5));
CREATE TABLE images_processed(time TIMESTAMP, entry INT(5));
CREATE TABLE images_anonymized(time TIMESTAMP, entry INT(5));

SELECT * FROM images_uploaded;
SELECT * FROM images_processed;
SELECT * FROM images_anonymized;


DELETE FROM images_uploaded;
DELETE FROM images_processed;
DELETE FROM images_anonymized;
INSERT INTO images_uploaded(time,entry) SELECT CURRENT_TIMESTAMP(), 0;
INSERT INTO images_processed(time,entry) SELECT CURRENT_TIMESTAMP(), 0;
INSERT INTO images_anonymized(time,entry) SELECT CURRENT_TIMESTAMP(), 0;
