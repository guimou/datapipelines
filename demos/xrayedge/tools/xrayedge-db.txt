mysql -u root

use xrayedgedb;

CREATE TABLE images_uploaded(time TIMESTAMP, name VARCHAR(255));
CREATE TABLE images_processed(time TIMESTAMP, name VARCHAR(255), model VARCHAR(10), label VARCHAR(20));
CREATE TABLE images_anonymized(time TIMESTAMP, name VARCHAR(255));

SELECT * FROM images_uploaded;
SELECT * FROM images_processed;
SELECT * FROM images_anonymized;


DELETE FROM images_uploaded;
DELETE FROM images_processed;
DELETE FROM images_anonymized;

INSERT INTO images_uploaded(time,name,entry) SELECT CURRENT_TIMESTAMP(), 'none', 0;
INSERT INTO images_processed(time,name,entry) SELECT CURRENT_TIMESTAMP(), 'none', 0;
INSERT INTO images_anonymized(time,name,entry) SELECT CURRENT_TIMESTAMP(), 'none', 0;
